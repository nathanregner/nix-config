# TODO

## Done

- [x] Unify `TreeState` and `RangerState` into single `ViewState` with shared `TreeListViewState<NodeIndex>`

- Add timestamp column
- Add sort by added, column, sizeAdd timestamp column
- Remove profiles hide option
- Check math.... added doesn't match closure at root?
- Also, closure is missing for some paths
- Does not redraw on resize
- add cache prune command
- add command for list of closures (non-gc roots)
- tree line numbers
- hard to see ranger sizes
- ranger bottom bar (like nix-tree)
- z{mM}? not workikng?
