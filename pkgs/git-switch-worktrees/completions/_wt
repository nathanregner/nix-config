#compdef wt

_wt() {
  local -a worktrees

  if git rev-parse --git-dir >/dev/null 2>&1; then
    worktrees=(${(@f)$(git-switch-worktrees list 2>/dev/null)})
    _describe 'worktree' worktrees
  fi
}

_wt "$@"
